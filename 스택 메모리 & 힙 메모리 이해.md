## 문제1.

```csharp
void UpdateList(int[] arr)
{
    arr[0] = 99;
}

int[] numArr = new int[] { 1, 2, 3 };
UpdateList(numbers);
Console.WriteLine(numbers[0]); 

// 출력 결과: ?
```

출력 결과를 적고, 왜 결과가 그렇게 나오는지 함수에 인자가 전달될 때
어떤 일이 일어나는지 메모리 관점(스택, 힙) 관점에서 설명해보세요.

---
## 문제 2.

```csharp
void ReplaceList(int[] arr)
{
    arr = new int[] { 9, 9, 9 };
}

int[] numArr = new int[] { 1, 2, 3 };
ReplaceList(numArr);
Console.WriteLine(data[0]); 

// 출력 결과: ?
```

출력 결과를 적고, 왜 결과가 그렇게 나오는지 메모리 관점(스택, 힙)에서 설명해보세요.

---
## 문제3.

예시 코드 1 — ref 붙어있지 않은 코드
```csharp
void Modify(int[] arr)
{
    arr[1] = 500;
}

int[] numArr = new int[] { 10, 20, 30 };
Modify(numArr);
Console.WriteLine(numArr[1]);

// 출력 결과: ?
```

예시 코드 2 — ref가 붙어있는 코드
```csharp
void ModifyRef(ref int[] arr)
{
    arr = new int[] { 7, 7, 7 };
}

int[] numArr2 = new int[] { 10, 20, 30 };
ModifyRef(ref numArr2);
Console.WriteLine(numArr2[0]); 

// 출력 결과: ?
```

위 두 가지 상황은 결과는 다릅니다.
예시 코드 1번과 2번의 출력 결과를 적고, 왜 동작 차이가 발생하는지 메모리 관점에서 설명해보세요.