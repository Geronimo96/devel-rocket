# 251210_이상훈 과제 

> 과제 1. 전쟁 게임의 일부 구현

- 아래의 내용을 출력하는 프로그램을 if문으로 제작하고, 이와 똑같은 내용으로 switch 문으로 제작하시오.
  - ReadLine을 통해 유저로부터 정수를 입력받기
    - 1을 입력받을 경우, "Cocked Pistol 발령"
    - 2일 경우, "Fast Pace 발령"
    - 3일 경우, "Round House 발령"
    - 이외에 대한 입력값은 "비상 태세"

## if 문으로 전쟁 게임 구현
```csharp

using System;

namespace _IF_Statement
{
    internal class Program
    {
        static void Main(string[] args)
        {  
            Console.Write("정수를 입력하시오. : ");
            int inputNum = int.Parse(Console.ReadLine());
            Console.WriteLine("\n----------\n");

            // if 문으로 전쟁 게임 구현
            if (inputNum == 1) Console.WriteLine("\"Cookend Pistol 발령\"");
            else if (inputNum == 2) Console.WriteLine("\"Fast Pace 발령\"");
            else if (inputNum == 3) Console.WriteLine("\"Round House 발령\"");
            else Console.WriteLine("\"비상 태세\"");         
        }
    }
}

```

## switch 문으로 전쟁 게임 구현
```csharp

using System;

namespace _IF_Statement
{
    internal class Program
    {
        static void Main(string[] args)
        {  
            Console.Write("정수를 입력하시오. : ");
            int inputNum = int.Parse(Console.ReadLine());
            Console.WriteLine("\n----------\n");

            //switch 문으로 전쟁 게임 구현
            switch (inputNum)
            {
                case 1:
                Console.WriteLine("\"Cookend Pistol 발령\"");
                break;

                case 2:
                Console.WriteLine("\"Fast Pace 발령\"");
                break;

                case 3:
                Console.WriteLine("\"Round House 발령\"");
                break;

                default:
                Console.WriteLine("\"비상 태세\"");
                break;
            }            
            
        }
    }
}

```

> 과제 2(심화/선택사항). switch expression
- MSDN : switch 식 - 키워드를 사용하여 switch 패턴 일치 식을 참고하여 switch 패턴 일치 식에 대한 사용법을 설명하시오.
- 해당 과제를 수행할 경우 제출하는 마크다운 문서에 함께 기재하여 제출합니다