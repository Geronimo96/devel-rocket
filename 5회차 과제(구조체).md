
> 과제 1. 변수 과제 리팩토링
'변수' 학습페이지의 1번 과제를 학생 1명의 단위에 대해 구조체 단위로 구현하시오.

### 📄 지원자 정보 요약

| 이름 | 나이 | 키 (cm) | 지원 동기 |
| :---: |  :---: |  :---: |  :---: |
| 김유훈 | 27 | 183.2 | 포트폴리오 완성도를 높이려고 지원했습니다. |
| 홍정옥 | 24 | 168.0 | 유튜브로 관심이 생겼는데 직접 게임까지 만들어보면 재밌을것같아서 지원했습니다. |
| 고병희 | 25 | 172.3 | 개발자가 멋있어보여서 지원하게 되었습니다. |
| 이인 | 만 24세 | 180.0 | 취직하고 싶어서 지원했습니다. |
| 김태형 | 28 | 177.0 | 취업 및 친구와 같이 게임을 만들고 싶어서 지원했습니다. |
---

```csharp
using System;

namespace Practice
{
    internal class Program
    {
        struct StudentData
        {
            public string Name;
            public int Age;
            public double Height;
            public string Motivation;

            // 구조체 초기화
            public StudentData(string name, int age, float height, string motivation)
            {
                Name = name;
                Age = age;
                Height = height;
                Motivation = motivation;
            }

            // 구조체 출력 함수
            public void PrintStudentInfo()
            {
                Console.WriteLine(" =================== ");
                Console.WriteLine($"이름 : {Name}");
                Console.WriteLine($"나이 : {Age} 살");
                Console.WriteLine($"키 : {Height:F1} cm");
                Console.WriteLine($"지원 동기 : {Motivation}");

            }
        }

        static void Main(string[] args)
        {
            StudentData [] studentDataArray = new StudentData[5];

            studentDataArray [0] = new StudentData("이인", 24, 180.0f, "취직하고 싶어서 지원했습니다.");
            studentDataArray [1] = new StudentData("고병희", 25, 172.3f, "개발자가 멋있어보여서 지원하게 되었습니다.");
            studentDataArray [2] = new StudentData("홍정옥", 24, 168.0f, "유튜브로 관심이 생겼는데 직접 게임까지 만들어보면 재밌을것같아서 지원했습니다.");
            studentDataArray [3] = new StudentData("김유훈", 27, 183.5f, "포트폴리오 완성도를 높이려고 지원했습니다.");
            studentDataArray [4] = new StudentData("김태형", 28, 177.0f, "취업 및 친구와 같이 게임을 만들고 싶어서 지원했습니다.");

            foreach (StudentData student in studentDataArray)
            {
                student.PrintStudentInfo();
            }

        }
    }
}

```

> 과제 2. 아이템과 인벤토리 구현
- 아이템과 인벤토리를 구현하시오.

 #### Item Struct
  - 내부 속성으로 이름과 고유 ID(int)를 가지며, 자신의 정보를 출력하는 기능을 가진다.
  
 #### Inventory Struct
  - 내부 속성으로 아이템을 관리하는 배열과, 배열에 들어있는 아이템의 정보를 전부 출력하는 기능을 가진다.

```csharp
using System;

namespace Practice
{
    internal class Program
    {
        //아이템과 인벤토리 구현
        //내부 속성으로 이름과 고유 ID(int)를 가지며, 자신의 정보를 출력하는 기능을 가진다

        // 게임 내에서 사용하는 단일 아이템을 표현하는 값
        struct Item
        {
            // 아이템의 고유 식별자
            public int Id;
            // 아이템의 이름
            public string Name;

            // Id와 Name을 초기화하는 생성자
            public Item(int id, string name)
            {
                Id = id;
                Name = name;
            }
            // 아이템 정보를 콘솔에 출력
            public void PrintItemInfo()
            {
                Console.WriteLine(" =================== ");
                Console.WriteLine("Item ID: " + Id);
                Console.WriteLine("Item Name: " + Name);
            }
        }

        //아이템 배열을 보관하고, 내부 아이템 정보를 모두 출력하는 인벤토리 구조체
        struct Inventory
        {
            // 인벤토리에 보관된 아이템 배열
            public Item[] Items;
            //아이템 배열로 인텐토리 초기화
            public Inventory(Item[] items)
            {
                Items = items;
            }

            // 인벤토리 내 모든 아이템 정보를 콘솔에 출력
            public void PrintInventoryInfo()
            {
                Console.WriteLine("===== Inventory =====");

                if (Items == null)
                {
                    Console.WriteLine("(인벤토리가 비어 있습니다.)");
                    return;
                }

                // 인벤토리 내 각 아이템 정보 출력 순회
                foreach (var item in Items)
                {
                    item.PrintItemInfo();
                }
            }
        }

        static void Main(string[] args)
        {
            //4개의 아이템을 담을 배열 생성
            Item[] Items = new Item[4];

            //배열에 아이템 할당 (ID, Name)
            Items[0] = new Item(1, "Sword");            //검   
            Items[1] = new Item(2, "Shield");           //방패
            Items[2] = new Item(3, "Health Potion");    //체력 물약
            Items[3] = new Item(4, "Magic Scroll");     //마법 두루마리

            //인벤토리 생성 및 아이템 배열 전달
            Inventory inventory = new Inventory(Items);
            //인벤토리 정보 출력
            inventory.PrintInventoryInfo();
        }
    }
}
```

> 과제 3(심화/선택사항)

```csharp
using System;
using System.Runtime.InteropServices;

class Program
{
    static void Main(string[] args)
    {
        int intSize = Marshal.SizeOf(typeof(int));
        Console.WriteLine(intSize); // 4 출력
        
        int doubleSize = Marshal.SizeOf(typeof(double));
        Console.WriteLine(doubleSize); // 8 출력
        
        int struct1Size = Marshal.SizeOf(typeof(MyStruct1));
        Console.WriteLine(struct1Size); // 16 출력
        
        int struct2Size = Marshal.SizeOf(typeof(MyStruct2));
        Console.WriteLine(struct2Size); // 16 출력
    }

    struct MyStruct1
    {
        private double a;
        private int b;
        private int c;
    }

    struct MyStruct2
    {
        private double a;
        private int b;
    }
}
```

- 위 소스코드의 출력 결과 상 Struct1, Struct2의 크기가 같게 출력되는 이유를 서술하시오